# issue 「cgo: commit hash欄」に表示されているlocal hashが古い値なのに、repo list側ではlocal clean と表示されてしまっている #189
[issues #189](https://github.com/cat2151/cat-repo-auditor/issues/189)

- そしてgキーでlazygitを起動してそこのlocal hashが古い値なのを確認してlazygitを抜けると、repo list側も「local clean」でなく「pullable」に表示更新される
- つまり、gキーで明示的にlocal checkを走らせるまで、local状態の変化の検知処理が漏れている
    - 仮説：cargo old状態のリポジトリは、backgroundで先行してcargo checkをする実装であるが、もしかして、そのとき、local状態のcheckを誤ってそのrepoで実施しないようなリグレッションがされているのでは？
        - local commit hashチェックは、cargo oldとは位置づけがまったく違うものである。cargo oldに関わらず、local commit hashチェックをするのはマストである。
- また、情報の位置づけがおかしい。cgo: commit hash欄は、remote hashと、installed hashを表示するものであるはず。local hashは、その上に新たに「local: commit hash」欄を表示するべきでは？
