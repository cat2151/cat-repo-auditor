# issue logの「cargo check: 」について、「repo name」「check対象filenameと、取得結果文字列のセット」「checkコマンドと、取得結果文字列のセット」もlogに出力する #95
[issues #95](https://github.com/cat2151/cat-repo-auditor/issues/95)

- 要は、「どのrepoに対して」「なにを（どのファイルをread、どのコマンドを実行）行った結果、なにを（どのような具体的な文字列を）取得できたのか？」
- をlogに出力する、ということ
- 現在のlogでは、データ不足で、調査には使えないので。

